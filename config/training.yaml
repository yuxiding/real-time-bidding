# config/training.yaml

algorithm: ppo
seed: 42
input_dim: 26
max_bid: 5.0
click_value: 50.0
episodes: 100
step_per_episode: 500
batch_size: 64
learning_rate: 0.001
gamma: 0.95
entropy_coef: 0.01
reward:
  type: roi_weighted
  use_entropy_bonus: true
